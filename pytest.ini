[pytest]
testpaths = tests
addopts = -m "not contract" -ra
markers =
    unit: pure logic, no network, no API keys
    integration: full subcommand coverage with mocked HTTP/subprocess, no keys
    contract: real API keys against live services (opt-in; run with -m contract)
